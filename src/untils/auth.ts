// export const checkAuthStatus = async (): Promise<boolean> => {
//   // Kiểm tra token, async storage, etc.
//   // Return true nếu đã login, false nếu chưa
//   return await new Promise(resolve =>
//     setTimeout(() => resolve(false), 1000)
//   );
// };
